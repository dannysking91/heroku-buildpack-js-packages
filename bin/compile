#!/bin/sh

BUILD_DIR=$1


ls -la "$BUILD_DIR"
ls -la "$BUILD_DIR/js-packages/"
cp -rf "$BUILD_DIR/js-packages/" "/js-packages"
cp -rf "$BUILD_DIR/js-packages/" "/tmp/js-packages"
ls -la /
ls -la /tmp/
